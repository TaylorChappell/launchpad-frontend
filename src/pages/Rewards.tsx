import { Gift, ShieldAlert } from "lucide-react";
import { Link } from "react-router-dom";
import { DEMO } from "../fixtures";
export function Rewards(){return <main className="page simple-page"><span className="eyebrow">STOCK AIRDROPS</span><h1>Holder rewards</h1><p>Reward-enabled markets route 1% of trading value into tokenized-stock purchases, then batch them into holder epochs.</p><div className="simple-stats"><Box l="Paid to holders" v="$601,790"/><Box l="Waiting to distribute" v="$20,088"/><Box l="Payouts" v="972,066"/></div><div className="rewards-layout"><section className="reward-list"><header>Reward markets</header>{DEMO.filter(x=>x.stockSymbol).slice(0,4).map(x=><div key={x.id}><span>{x.stockSymbol?.slice(0,2)}</span><div><b>{x.name} <small>${x.symbol}</small></b><small>Earn {x.stockSymbol} · {x.holderCount.toLocaleString()} holders</small></div><strong>{x.rewardVaultStock.toFixed(3)} {x.stockSymbol}</strong><Link to={`/token/${x.id}`}>Market</Link></div>)}</section><aside><Gift/><h2>How it works</h2><ol><li>The contract separates the reward fee from platform revenue.</li><li>A keeper swaps accumulated SOL into the selected xStock.</li><li>Balances are snapshotted into a Merkle epoch.</li><li>Eligible wallets claim directly.</li></ol><div className="legal"><ShieldAlert/>Tokenized stocks may be restricted in your location.</div></aside></div></main>}function Box({l,v}:{l:string;v:string}){return <div><small>{l}</small><b>{v}</b></div>}
