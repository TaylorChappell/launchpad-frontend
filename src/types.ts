export type RuntimeConfig = { useTestnet: boolean; network: "devnet" | "mainnet-beta"; publicRpcUrl: string; programId: string | null; transactionsEnabled: boolean; fees: { platformBps: number; rewardsBps: number }; graduationSol: number };
export type StockOption = { symbol: string; name: string; mint: string; accent: string; restricted: true; halted: boolean };
export type Launch = { id: string; mint: string; creatorWallet: string; name: string; symbol: string; description: string; imageUrl: string; stockSymbol: string | null; stockName: string | null; stockMint: string | null; status: "curve" | "graduating" | "orca"; progress: number; priceUsd: number; marketCapUsd: number; volume24hUsd: number; change24h: number; holderCount: number; realSolReserve: number; virtualSolReserve?: number; virtualTokenReserve?: number; rewardVaultStock: number; rewardDistributedUsd: number; devBuySol: number; devSupplyPct: number; txCount: number; xUrl?: string | null; websiteUrl?: string | null; telegramUrl?: string | null; orcaPool?: string | null; createdAt: number };
export type Trade = { id: string; wallet: string; side: "buy" | "sell"; solAmount: number; tokenAmount: number; priceUsd: number; createdAt: number };
export type RewardEpoch = { id: string; launchId: string; stockSymbol: string; stockMint: string; merkleRoot: string; totalStockAmount: number; totalUsd: number; eligibleHolders: number; snapshotSlot: number; status: string; createdAt: number };
